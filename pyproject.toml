[project]
name = "moldiff"
version = "0.1.0"
description = "Diffusion-based molecular generation with protein docking"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["src*"]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
